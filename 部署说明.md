# BizEnglish Coach — 上线部署说明

这个 `deploy/` 文件夹就是要上线的全部内容。部署到 HTTPS 后：

- 电脑 / 手机浏览器都能直接打开
- **语音识别**能正常工作（file:// 下不行的功能，上线后就好了）
- 可以"添加到主屏"，像 App 一样全屏使用、离线打开

---

## 方式一：Netlify Drop（最简单，推荐，全程约 3 分钟）

无需注册账号、无需命令行，把文件夹拖进网页即可。

1. 用浏览器打开 **[https://app.netlify.com/drop](https://app.netlify.com/drop)**
2. 打开文件夹 `deploy/`（就是本说明所在的文件夹）
3. 把 `deploy`** 文件夹整个拖进** Netlify 网页中间的虚线框
4. 等几秒，Netlify 会给你一个网址，形如 `https://随机名字.netlify.app`
5. 点开这个网址 —— 就是你的在线 App 了！ （想改成好记的名字：注册一个免费 Netlify 账号 → Site settings → Change site name）

> 拖拽时请确保拖的是文件夹里的**内容**（index.html 在最外层）。 如果拖进去后打开是文件列表而不是应用，说明多套了一层文件夹——把 index.html 那一层拖进去即可。

---

## 方式二：GitHub Pages（适合想长期维护）

1. 在 GitHub 新建一个仓库
2. 把 `deploy/` 里的所有文件上传到仓库根目录
3. 仓库 Settings → Pages → Source 选 `main` 分支 `/root`
4. 几分钟后得到 `https://你的用户名.github.io/仓库名/`

---

## 方式三：Vercel（也很简单）

1. 打开 [https://vercel.com](https://vercel.com) 注册（可用 GitHub 登录）
2. New Project → 上传 `deploy/` 文件夹 → Deploy
3. 得到 `https://xxx.vercel.app`

---

## 部署后怎么用 / 验证

**在电脑上**：打开分配的 https 网址 → 走到「口语回应」→ 点麦克风 → 允许 → 说话，这次**语音识别应该能出文字**了。

**在手机上（添加到主屏，像 App）**：

- iPhone Safari：打开网址 → 分享按钮 → "添加到主屏幕"
- 安卓 Chrome：打开网址 → 菜单 → "安装应用 / 添加到主屏幕"
- 之后从主屏图标打开，就是全屏、无浏览器地址栏的 App 体验，也支持离线打开

## 文件清单（deploy 文件夹内）

- index.html — 主应用
- manifest.json — PWA 配置
- sw.js — 离线缓存 Service Worker
- icon-192.png / icon-512.png — App 图标
- netlify.toml — Netlify 头部配置（其他平台可忽略）

---

## 常见问题

- **麦克风还是不能用？** 确认网址是 `https://` 开头，并在弹窗里点了"允许"。地址栏左侧小锁图标里可管理麦克风权限。
- **iPhone 上识别不出文字？** iOS Safari 的浏览器语音识别支持较弱——录音和回放正常，识别可能仍不稳定。若要 iPhone 上稳定识别，未来需接专业云端语音服务（正式版规划）。
- **改了内容后网址没更新？** Service Worker 有缓存，强制刷新（Ctrl+Shift+R）或在浏览器里清一下该站点缓存。

